# Kite

Kite is a 3D rigid body simulator. This repository contains the code for the main library `kite_core`, a basic simulator on top of the above library `kite_sim` and a python binding `kite_python_binding`.
